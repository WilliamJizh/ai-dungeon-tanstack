import { describe, it } from 'vitest';

describe('ChoiceFrame', () => {
  it.todo('renders one button per choice');
  it.todo('first choice has selected styling by default');
  it.todo('clicking choice fires onChoiceSelect(id)');
  it.todo('free-text input row renders when showFreeTextInput=true');
  it.todo('submitting free text fires onFreeTextSubmit(text)');
});
